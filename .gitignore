# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~

# Dependency / build artifacts (not used, but kept for hygiene)
node_modules/
.cache/
.vercel/

# Installed Skill packages (regenerated via `npx skills experimental_install` from skills-lock.json)
.agents/
.claude/

# Local env
.env
.env.local

# Private / source assets (kept local, not published on the public site)
Lebenslauf_Henri_Loehlein.pdf
*.pdf
Bilder/

# Internal working docs (kept local, not published on the public repo —
# contain name/personal notes not meant to surface when someone googles Henri)
README.md
DEPLOY.md
PROJEKT-STATUS.md
REFERENZEN.md
CLAUDE.md

# Dev/experiment preview pages (not part of the live site, not for public repo)
*preview*
