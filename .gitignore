# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~

# Dependency / build artifacts (not used, but kept for hygiene)
node_modules/
.cache/
.vercel/

# Installed Skill packages (regenerated via `npx skills experimental_install` from skills-lock.json)
.agents/
.claude/

# Local env
.env
.env.local

# Private source material (never published: CV with phone number and date of birth,
# original image folder)
Lebenslauf_Henri_Loehlein.pdf
*.pdf
Bilder/
